# Installs the MySQL client libraries. Suitable for application and
# search servers where there is no need to run the MySQL server.
#

install_package 'libmysqlclient-dev'
install_package 'mysql-client'
